# 🌦️ Dynamic Weather Forecast CLI (Python)

A simple Python script that fetches **current weather** and **7-day forecast** for any city using the **WeatherAPI**.  
User-friendly, dynamic, and safe with `.env` for your API key.

---

## 🚀 Features

- Fetch **current temperature**, **condition**, **sunrise/sunset**, and more.  
- Fetch a **7-day weather forecast**.  
- User can input **any city** dynamically.  
- Secure API handling using `.env`.  
- Clean, readable output for terminal (CLI).

---

## 🛠️ Tech Stack

- Python 3.x  
- Libraries:
  - `requests`
  - `python-dotenv`  
- WeatherAPI (free tier)

---

## 📦 Installation

1. Clone the repo:

- git clone git@github.com:Aayam-Rimal/weather-CLI.git

2. Activate your virtual environment

3. Install dependencies
- pip install -r requirements.txt

---

## 🔑 Setup Environment Variables

- create a .env file and save your keys there

## Run the script in your local machine to get all the data or edit as you need!





